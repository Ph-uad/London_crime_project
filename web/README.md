# Web Frontend

This directory contains the Next.js frontend scaffold for the dashboard.

Current status:
- Basic app scaffold with TypeScript, React, and placeholder pages.
- No dashboard map, API routes, or final UI components implemented yet.

Next steps:
- Add data API routes under `web/pages/api/` or `web/app/api/`.
- Build the choropleth map and metric controls.
- Connect the frontend to `data/processed/` outputs or API data.
